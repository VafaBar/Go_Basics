package main

import "fmt"

func main() {

	v := []int{1, 2, 3, 4}

	fmt.Println(len(v), cap(v))

	// Создайте слайс и добавьте в него несколько элементов.
	// Используйте функции len и cap для определения длины и вместимости слайса. Выведите эти значения на экран.
}
